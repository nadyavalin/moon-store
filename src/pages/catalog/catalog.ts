export function renderCatalogContent() {
  const loginFormText = "This is the Catalog page!";
  return `
      <div class="catalog-page">
          ${loginFormText}
      </div>
  `;
}

export default renderCatalogContent;
